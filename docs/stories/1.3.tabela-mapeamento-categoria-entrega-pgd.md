# Story 1.3: Tabela de Mapeamento Categoria -> Entrega PGD

## Status
Ready for Review

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "tests", "manual-architecture-review"]

## Story
**As a** coordenador da CGTE,  
**I want** que o mapeamento entre categorias do Kanboard e entregas do PGD seja configuravel,  
**so that** eu possa ajustar sem precisar de suporte tecnico.

## Acceptance Criteria
1. Arquivo de configuracao `config/taxonomy.json` com as 18 categorias e suas entregas PGD vinculadas
2. Tipo TypeScript `TaxonomyConfig` definido com validacao de schema
3. Funcao `getTaxonomy()` que carrega e valida a configuracao
4. Funcao `getEntregasPGD(categoriaId)` que retorna as entregas vinculadas a uma categoria
5. Funcao `getCategoriasByEntrega(entregaId)` que retorna as categorias que alimentam uma entrega
6. Testes unitarios cobrindo mapeamento correto, categoria inexistente e configuracao invalida

## Tasks / Subtasks
- [x] Criar `config/taxonomy.json` com estrutura inicial das 18 categorias (AC: 1)
- [x] Definir tipo `TaxonomyConfig` e schema de validacao (AC: 2)
- [x] Implementar carregamento e validacao em `getTaxonomy()` (AC: 3)
- [x] Implementar consulta por categoria: `getEntregasPGD(categoriaId)` (AC: 4)
- [x] Implementar consulta por entrega: `getCategoriasByEntrega(entregaId)` (AC: 5)
- [x] Criar testes unitarios de mapeamento e validacao (AC: 6)

## Dev Notes
- Fonte: `docs/prd.md` (Story 1.3), `docs/architecture.md` (Taxonomy Service)
- Config deve ser editavel e versionada no repositorio
- Evitar qualquer hardcode fora da camada de configuracao

### Testing
- Cenarios obrigatorios:
  - config valida
  - categoria inexistente
  - config invalida (schema)
- Rodar: `npm run lint`, `npm run typecheck`, `npm test`

## Story Checklist
- [ ] Story revisada contra PRD
- [ ] Tasks cobrem todos os ACs
- [ ] Story pronta para desbloquear Story 1.4 e Epic 3

## Dev Agent Record
### File List
- `config/taxonomy.json`
- `types/kanboard.ts`
- `lib/taxonomy.ts`
- `tests/unit/taxonomy.test.ts`

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-24 | 0.1 | Criacao inicial da story | @sm/@architect |
| 2026-02-24 | 1.0 | Implementacao concluida e validada (lint/typecheck/test/build) | @dev |
