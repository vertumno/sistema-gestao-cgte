# Story 1.2: Servico de Integracao com API do Kanboard

## Status
Ready for Review

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "tests", "manual-architecture-review"]

## Story
**As a** desenvolvedor,  
**I want** um servico que consome a API JSON-RPC do Kanboard,  
**so that** o sistema possa ler tarefas, categorias e projetos do board da CGTE.

## Acceptance Criteria
1. Modulo `lib/kanboard-client.ts` implementado com client JSON-RPC 2.0 tipado
2. Metodos implementados: `getAllTasks()`, `getTasksByProject()`, `getAllCategories()`, `getProjectById()`, `getAllSubtasks()`
3. Autenticacao via API token configurada por variavel de ambiente
4. Tratamento de erros com mensagens claras (timeout, auth failure, connection refused)
5. Testes unitarios com mocks para todos os metodos
6. Rate limiting basico para nao sobrecarregar o Kanboard (max 10 req/s)

## Tasks / Subtasks
- [x] Implementar `lib/kanboard-client.ts` com tipagem de request/response (AC: 1)
- [x] Implementar metodos de leitura exigidos (AC: 2)
  - [x] `getAllTasks()`
  - [x] `getTasksByProject(projectId)`
  - [x] `getAllCategories()`
  - [x] `getProjectById(projectId)`
  - [x] `getAllSubtasks(taskId?)`
- [x] Integrar token de autenticacao por env var (AC: 3)
- [x] Implementar padrao de erro com timeout/rede/auth (AC: 4)
- [x] Implementar rate limiting 10 req/s (AC: 6)
- [x] Criar testes unitarios com mocks da API externa (AC: 5)

## Dev Notes
- Fonte: `docs/prd.md` (Story 1.2), `docs/architecture.md` (component Kanboard Client Adapter)
- Nao expor token em client-side
- Integracao deve ficar encapsulada em camada adapter/repository

### Testing
- Cobertura de testes para todos os metodos publicos
- Casos minimos: sucesso, timeout, auth failure, connection refused, rate limit
- Rodar: `npm run lint`, `npm run typecheck`, `npm test`

## Story Checklist
- [ ] Story revisada contra PRD
- [ ] Tasks cobrem todos os ACs
- [ ] Dependencias para Story 1.4 mapeadas

## Dev Agent Record
### File List
- `types/kanboard.ts`
- `lib/kanboard-client.ts`
- `tests/unit/kanboard-client.test.ts`

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-24 | 0.1 | Criacao inicial da story | @sm/@architect |
| 2026-02-24 | 1.0 | Implementacao concluida e validada (lint/typecheck/test/build) | @dev |
